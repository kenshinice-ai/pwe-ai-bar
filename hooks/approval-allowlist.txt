# PWE AI Bar — remote approval allowlist.
#
# One entry per line: "Tool" or "Tool:prefix". A prefix matches the command (Bash) or the file
# path (Read/Edit/Write). Anything not listed here is never answered from a notification — it
# goes to Claude Code's own prompt, in the terminal, where you can see the whole context.
#
# Keep this narrow. The point of answering remotely is to unblock work you already expected,
# not to approve things you have not looked at.

Bash:git status
Bash:git diff
Bash:git log
Bash:npm test
Bash:swift build
